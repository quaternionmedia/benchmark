{
  "name": "benchmark",
  "version": "0.1.0",
  "description": "A frontend-only map of benches worldwide.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "validate": "node scripts/compile-yaml.js",
    "validate:fix": "node scripts/compile-yaml.js --fix",
    "catalogue": "node scripts/generate-catalogue.js",
"overpass-import": "node scripts/overpass-import.js",
    "test": "playwright test"
  },
  "dependencies": {
    "animejs": "^3.2.2",
    "leaflet": "^1.9.4",
    "leaflet.markercluster": "^1.5.3",
    "yaml": "^2.8.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "vite": "^7.3.1"
  }
}
